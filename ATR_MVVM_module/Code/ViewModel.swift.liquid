{% include 'header' %}
import Foundation
import RxSwift

protocol {{ module_info.name }}VMProtocol {
    func viewIsReady()
}

final class {{ module_info.name }}VM {
    private let disposeBag = DisposeBag()

    let router: AppRouterProtocol
    init(router: AppRouterProtocol) {
        self.router = router
    }
}

extension {{ module_info.name }}VM: {{ module_info.name }}VMProtocol {
    func viewIsReady() {
    }
}
